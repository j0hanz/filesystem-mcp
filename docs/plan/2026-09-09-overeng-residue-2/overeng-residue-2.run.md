# Run: cut the 22 verified over-engineering residues left by the 2026-09-09 audit

Executing [`overeng-residue-2.plan.md`](overeng-residue-2.plan.md), started 2026-09-09 at `f7e47a1d`.

- **Drift** 2026-09-09 — clean: HEAD `f7e47a1d`, `git diff --stat f7e47a1d..HEAD -- src/ __tests__/` empty, tree clean. Starting at step 1.
- **1** 2026-09-09 — done. Interface deleted, resolver returns `string[]`, `initialize(readonly string[])`, four `.expanded` reads dropped, comment adjusted. `node scripts/tasks.mjs test` → pass 277 fail 0.
- **2** 2026-09-09 — done. `Promise.all` + index filter; one `root` → `_` rename for TS6133. `git grep allSettled src/core/path.ts` → empty.
- **3** 2026-09-09 — done. `private readonly options`. `node scripts/tasks.mjs fix` → exit 0 (prettier reflowed path.ts).
- **4** 2026-09-09 — done. Knobs deleted from `ReadSpec`/`NormalizedBase`/`ReadContentOptions`; `buildBaseOptions` shrunk; encoding reads → `'utf-8'`; `assertNotBinary` unconditional; `ReadSpecCommon` deleted; fs.ts spec shrunk. Deviation: also dropped `readFullContent`'s `encoding` param (sole caller passed `'utf-8'`) so the plan's `BufferEncoding`-free gate holds. Tests → pass 277 fail 0; both grep gates → empty.
- **5** 2026-09-09 — done. `truncated` deleted from both interfaces and all 10 sites; write-only range-path `stoppedByLimit` (tail-path one kept — live reads at 485/493); two duplicate test asserts deleted. `git grep truncated src/core/read.ts __tests__/core-fs.test.ts` → empty; tests → pass 277 fail 0.
- **6** 2026-09-09 — done. `SearchResult.column` added; `countLineMatches` → `findLineMatches` returning `{count, column}`; `findColumnOffset`/`SearchContext`/context plumbing deleted; payload sets `column` always; call site `buildSortedPayloads(result)`. Gate run exact (`interface SearchContext|findColumnOffset|countLineMatches` → empty — bare `SearchContext` substring hits unrelated `getSearchContext` in path-completer.ts). Tests → pass 277 fail 0.
- **7** 2026-09-09 — done. Both summary types narrowed to `'maxResults' | 'timeout'` with narrowing comments; both `resolve()` sites cast with invariant comment; unused `StoppedReason` type import dropped; both `!== 'maxFiles'` guard filters deleted. `git grep "!== 'maxFiles'" src/` → empty; `--quick` exit 0 after prettier reflow; tests → pass 277 fail 0.
- **8** 2026-09-09 — done. Destructured `mimeType` from `readRaw`; lossy utf-8 roundtrip and `detectMimeFromContent` import deleted. Tests → pass 277 fail 0.
- **9** 2026-09-09 — done. `count()` deleted from interface + both matchers; both `replace()`s count in their own callback and return `{content, matchCount}`; `buildReplacementPlan` single-pass, zero-match contract kept; unused `patternLength` dropped. `git grep "\.count(" src/tools/replace-in-files.ts` → empty; tests → pass 277 fail 0.
- **10** 2026-09-09 — done. `!ctx` fallback branch deleted; `toToolCtx(ctx: ServerContext, …)` non-optional. Tests → pass 277 fail 0.
- **11** 2026-09-09 — done. `ToolCtx.authInfo` field + spread + `AuthInfo` import deleted; whole `auth_probe` it-block (defineTool probe + HTTP + in-memory exercises) deleted from tools.test.ts; http-policy.ts comment reworded (code untouched). `git grep auth_probe` → empty. Deviation: test count now 276 (277 − 1 deleted test); `pass 276 fail 0`.
- **12** 2026-09-09 — done. `resolveProgressCtx` try/catch + `Logger.warn` deleted; one-line ternary. `git grep "progress threw"` → empty; `Logger` import kept (still used by `ctx.log`). Tests → pass 276 fail 0.
- **13** 2026-09-09 — done. `regexCache` deleted; `findEditMatch` compiles per edit with `freeRegex` in try/finally; `applyEdits` plain loop; unused `Regex` type import dropped. `git grep regexCache src/` → empty.
- **14** 2026-09-09 — done. `buildSummary` `skipped` param required, default deleted. Deviation: step 11's auth_probe deletion left 7 unused imports in tools.test.ts (TS6133, caught by `--quick` exit 2) — removed (`StreamableHTTPClientTransport`, `createMcpHandler`, `McpServer`, `z`, `PageSnapshotStore`, `ResourceStore`, `defineTool`, `makeGuard`). `--quick` exit 0; tests → pass 276 fail 0.
- **15** 2026-09-09 — done. `assertDirectory` inlined into `validateDirectoryPath`; `allowMissing` now returns early on any stat failure. `git grep "is not a directory" src/cli.ts` → exactly one hit; tests → pass 276 fail 0.
- **16** 2026-09-09 — done. `keepForceExitTimer` flag, outer catch `shutdown_error` handler, and the `if (!keepForceExitTimer)` guard deleted; `finally { clearTimeout(timer); }` unconditional. `git grep keepForceExitTimer\|shutdown_error src/` → empty; tests → pass 276 fail 0.
- **17** 2026-09-09 — done. try/catch around get-help handler body deleted (body kept verbatim); `ProtocolError` import and `./core/errors.js` import deleted; `Logger` kept (line 74 debug). `git grep ProtocolError\|hasErrorShape src/prompts.ts` → empty; tests → pass 276 fail 0.
- **18** 2026-09-09 — done. `compilePatterns` merged into `toPatternSet`; `CompiledPattern` interface deleted; ctor calls `toPatternSet(patterns)` directly. `git grep "CompiledPattern\b\|compilePatterns" src/` → empty; tests → pass 276 fail 0.
- **19** 2026-09-09 — done. `findRootPrefixMatches` now derives the prefix via `parseNamedRootInput(...)?.rootName`; `getRootPrefix` deleted. `git grep getRootPrefix src/` → empty; tests → pass 276 fail 0.
- **20** 2026-09-09 — done. `defaultValue` deleted from `PendingInput`, `choiceInput`, and `buildInputRequired`'s schema spread; no callers passed it. `git grep defaultValue src/core/input-required.ts` → empty; tests → pass 276 fail 0.
- **21** 2026-09-09 — done. `ResourceEntry.storedAt` field + assignment deleted; local `const storedAt` kept (`expiresAt` reads it). `git grep storedAt src/ __tests__/` → only local + expiresAt use; `--quick` exit 0 after prettier reflowed step 17's prompts.ts.
- **22** 2026-09-09 — done. Pre-check: every `(err|error|e).path` hit is a `Pick<Problem>` (formatDetailedError) or a plain result object, no FsError receiver. `get path()` deleted, `get code()` kept. `git grep "get path" src/core/errors.ts` → empty; tests → pass 276 fail 0.
- **QC fix** 2026-09-09 — done. `workbench:qc` blocked on `ReadContentOptions` (byte-identical to `NormalizedBase`) + `buildReadContentOptions` pass-through, residue of step 4: interface + converter deleted, `readLinesBounded`/`readRangeContent`/`readTailContent` take `NormalizedBase`, three call sites pass `spec` directly. `git grep "ReadContentOptions\|buildReadContentOptions"` → empty; `--quick` exit 0; tests → pass 276 fail 0.

## Done

- [x] `node scripts/tasks.mjs --quick` → exit 0.
- [x] `node scripts/tasks.mjs test` → pass 276 fail 0 (277 − 1 deleted auth_probe test; steps 1-10 verified at 277 first).
- [x] `git status` → 22 modified files, all in the plan's in-scope list; no out-of-scope file touched.
- [x] All grep gates empty: step 4 (`skipBinary` in read.ts, `BufferEncoding` in core/read.ts + tools/read.ts), step 5 (`truncated` in core/read.ts + core-fs.test.ts), step 6 (`SearchContext`/`findColumnOffset`/`countLineMatches`), step 11 (`auth_probe`), step 13 (`regexCache`), step 15 (`is not a directory` in cli.ts → exactly one hit), step 16 (`keepForceExitTimer`/`shutdown_error`), step 17 (`ProtocolError`/`hasErrorShape` in prompts.ts), step 18 (`CompiledPattern`/`compilePatterns`), step 19 (`getRootPrefix`), step 20 (`defaultValue` in input-required.ts), step 22 (`get path` in errors.ts).